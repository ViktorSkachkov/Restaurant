stages:
  - build
  - test

build:
  stage: build
  script:
    - cd Project
    - ./gradlew build -x test
    - ./gradlew.bat --stop

test:
  stage: test
  script:
    - cd Project
    - ./gradlew test
    - ./gradlew.bat --stop
