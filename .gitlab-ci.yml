stages:
  - build
  - test

build:
  stage: build
  script:
    - cd Project/demo
    - ./gradlew build -x test
    - ./gradlew.bat --stop

test:
  stage: test
  script:
    - cd Project/demo
    - ./gradlew test
    - ./gradlew.bat --stop
