stages:
  - build
  - test

build:
  stage: build
  script: 
    - ./gradlew build -x test
    - ./gradlew.bat --stop

sonarqube-check:
  stage: test
  image: gradle:alpine
  script: gradle sonarqube -Dsonar.qualitygate.wait=true
  allow_failure: true
  only:
    - main

